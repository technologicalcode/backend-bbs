/** Generación de slots de cita a partir de horarios de atención. */
export const CITA_GENERATOR = Symbol('CITA_GENERATOR');

/** Persistencia de citas (creación en lote). */
export const CITAS_WRITER = Symbol('CITAS_WRITER');
